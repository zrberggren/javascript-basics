// You can use parentheses to control the order of operations in an expression. For example, the following expression evaluates to 20, not 14, because the multiplication operator has higher precedence than the addition operator:
let x = (2 + 3) * 4;
console.log(x);
